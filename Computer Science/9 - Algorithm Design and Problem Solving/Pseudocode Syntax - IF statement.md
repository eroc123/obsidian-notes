---
tags:
  - "#pseudocode"
  - "#algorithmdesign"
---

Name <- "John"
IF Name = "John"
	THEN
		PRINT Name
	ELSE
		PRINT "Not the right name"
ENDIF

For an IF statement, there must be a condition (Comparison in the if)
